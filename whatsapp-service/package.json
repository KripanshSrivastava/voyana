{
  "name": "voyana-whatsapp-service",
  "version": "0.1.0",
  "private": true,
  "description": "Internal-only service that owns the open-wa WhatsApp Web session and exposes it to the main app over the Docker network. See docs/WHATSAPP.md.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@open-wa/wa-automate": "^4.66.0",
    "express": "^4.21.2"
  }
}
